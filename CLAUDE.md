@AGENTS.md

## Claude Code

- Prefer `.claude/rules/` with `paths:` frontmatter over growing `AGENTS.md`.
- Verify instruction loading with `/context`; `CLAUDE.md` must appear under **Memory files**.
